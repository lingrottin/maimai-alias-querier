<%
    _pages=[["","<i class='fa-solid fa-magnifying-glass'></i>&nbsp;查询"],["submit",'<i class="fa-solid fa-table"></i>&nbsp;提交'],['review','<i class="fa-solid fa-list-check"></i>&nbsp;审核']];
    var pages=[];
    var curPage;
    _pages.forEach(value=>{
        if(value[0]!=page){
            pages.push(value);
        }else{
            curPage=value;
        }
    })
%>

<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <!-- Container wrapper -->
    <div class="container-fluid m-1">
        <!-- Navbar brand -->
        <a class="navbar-brand mt-2 mt-lg-0 me-auto text-center" href="/">
            <i class="fa-solid fa-database"></i>
            &nbsp;Maimai Alias Querier
        </a>
        <!-- Toggle button -->
        <button class="navbar-toggler" type="button" data-mdb-toggle="collapse"
            data-mdb-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
        </button>

        <!-- Collapsible wrapper -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <!-- Left links -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/<%-curPage[0]%>"><%-curPage[1]%></a>
                </li>
                <% pages.forEach(value=>{ %>
                    <li class="nav-item">
                        <a class="nav-link" href="/<%-value[0]%>"><%-value[1]%></a>
                    </li>
                <% }); %>
            </ul>
            <!-- Left links -->
            <!-- Right elements -->
            <div class="d-flex align-items-center">
                <!-- Icon -->
                <i class="fa-regular fa-clock"></i>
                &nbsp;
                <label id="navbar-date"><%- (new Date()).toLocaleString(); %></label>
            </div>
            <!-- Right elements -->
        </div>
        <!-- Collapsible wrapper -->


    </div>
    <!-- Container wrapper -->
</nav>
<!-- Navbar -->